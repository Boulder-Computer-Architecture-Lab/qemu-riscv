// RISCV Translation routines for SHREWD fault tolerance instructions.

/* Handles secon */
static bool trans_secon(DisasContext *ctx, arg_empty *a)
{
    return true;
}

/* Handles secoff */
static bool trans_secoff(DisasContext *ctx, arg_empty *a)
{
    return true;
}